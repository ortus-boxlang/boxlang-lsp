/**
 * Child class that extends Parent.
 */
class extends="Parent" {

	function childFunc() {
		// This should go to parentFunc() in Parent.bx (without this)
		parentFunc();
	}

	function childFuncWithThis() {
		// This should also go to parentFunc() in Parent.bx (with this)
		this.parentFunc();
	}

}
